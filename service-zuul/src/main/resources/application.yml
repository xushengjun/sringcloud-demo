server:
  port: 8886

spring:
  application:
    name: service-zuul
  profiles:
    active:
      - dev


zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-ribbon
    api-b:
      path: /api-b/**
      serviceId: service-feign